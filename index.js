/**
 *
 *
 * -e|--entry       Entry point, can be file or directory. If empty, assumes current working directory
 * -o|--output      Result file ends up here
 *
 * --use-require    Looks for node-style `require` (default: false)
 * --use-import     Looks for ES2015+ `import`  (default: true)
 */
const lumberjack = require('./lib/lumberjack');


